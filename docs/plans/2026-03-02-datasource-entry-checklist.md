# 数据源接入入口改造任务清单（默认关闭，保留本地文件模式）

> 目标：先具备真实数据源接入入口（配置 + 连接器桩），但默认不启用；当前分析流程继续使用本地文件/本地资产。

## 任务清单

- [x] T1 扩展工具配置模型：新增 `telemetry_source`、`cmdb_source` 配置项（默认 `enabled=false`）。
- [x] T2 新增连接器入口：实现 `TelemetryConnector`、`CMDBConnector`（先做通用 HTTP 拉取桩 + 错误降级）。
- [x] T3 集成到 Agent 工具上下文：在 `MetricsAgent`、`DomainAgent` 路径按开关尝试远程数据源；关闭时完全走本地逻辑。
- [x] T4 前端设置页新增配置入口：展示两个数据源配置开关与参数编辑表单，默认关闭。
- [x] T5 自测与回归：后端编译 + 前端构建 + 核心接口检查，确认现有本地模式不受影响。

## 执行记录

- 2026-03-02：清单创建。
- 2026-03-02：T1-T5 已完成（默认关闭远程入口，保持本地模式）。
